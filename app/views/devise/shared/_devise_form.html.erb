<div class="flex flex-col gap-10">
  <div class='flex flex-col gap-10'>
    <% unless local_assigns[:form_title].nil? %>
      <h1><%= form_title %></h2>
    <% end %>
    <%= yield %>
  </div>
  <hr/>
  <%= render "devise/shared/links" %>
</div>
