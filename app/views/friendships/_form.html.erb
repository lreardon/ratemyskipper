<%= form_with(model: friendship, class: "contents") do |form| %>
  <%= render 'shared/errors' resource: friendship %>
  <div class="inline">
    <%= form.submit class: "rounded-lg py-3 px-5 bg-blue-600 text-white text-lg cursor-pointer" %>
  </div>
<% end %>
