<div class="mx-auto w-full">
  <h1 class="font-bold text-4xl">Editing friendship</h1>

  <%= render "form", friendship: @friendship %>

  <%= render 'shared/button/link', title: 'Show this friendship', path: @friendship, twcss_color: 'bg-gray-100' %>
  <%= render 'shared/button/link', title: 'Friendships', path: friendships_path, twcss_color: 'bg-gray-100' %>
</div>
