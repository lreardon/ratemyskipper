<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">New friendship</h1>

  <%= render "form", friendship: @friendship %>

  <%= render 'shared/button/link', title: 'Back to friendships', path: friendships_path, twcss_color: 'bg-gray-100' %>
</div>