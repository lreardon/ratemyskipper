<%= form_with(model: skipper, class: "contents") do |form| %>
  <%= render 'shared/errors', resource: skipper %>
  <div class='flex flex-col justify-start gap-6'>
    <div>
      <%= form.label :firstname, 'First name' %>
      <%= form.text_field :firstname %>
    </div>
    <div>
      <%= form.label :lastname, 'Last name' %>
      <%= form.text_field :lastname %>
    </div>
    <div>
      <%= form.label :boatname, "Boat name (exclude F/V)" %>
      <%= form.text_field :boatname %>
    </div>
    <!--
      <div class='flex flex-col xs:flex-row gap-6'>
        <%= form.label :fishery, 'Fishery' %>
        <div class='flex flex-col xs:flex-row gap-3'>
          <% Fisheries.constants.map { |c| Fisheries.const_get c }.each do |fishery| %>
            <div>
              <%= form.radio_button :fishery, fishery %>
              <%= form.label :fishery, fishery %>
            </div>
          <% end %>
          <div>
            <%= form.radio_button :fishery, '' %>
            <%= form.label :fishery, 'Other' %>
            <%= form.text_field :fishery, value: '' %>
          </div>
        </div>
      </div>
    -->
    <%= form.submit class: "rounded-lg py-3 px-5 bg-blue-100 text-lg cursor-pointer" %>
  </div>
<% end %>
