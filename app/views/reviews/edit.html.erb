<div class="flex flex-col mx-auto md:w-2/3 w-full gap-10">
  <div class='flex flex-row justify-end'>
    <%= link_to 'Back to Skipper', skipper_path(@skipper), class: "ml-2 rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium" %>
  </div>
  <h1 class="font-bold text-4xl">Editing review</h1>
  <%= render "form", review: @review %>

  <%= button_to 'Delete Review', review_path(@review), method: :delete, class: "rounded-lg py-3 px-5 bg-red-100 font-medium" %>
</div>
