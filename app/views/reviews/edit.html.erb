<div class="flex flex-col w-full gap-16">
  <div class='flex flex-row'>
    <%= render 'shared/button/link', title: 'Back to skipper', path: @skipper, twcss_color: 'bg-gray-100' %>
  </div>
  <h1 class="font-bold text-4xl">Editing Review</h1>
  <%= render "form", review: @review %>
  <%= render 'shared/button/form', title: 'Delete Review', path: @review, method: :delete, twcss_color: 'bg-red-100' %>
</div>