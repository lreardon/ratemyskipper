<div class="w-full">
  <div class="flex justify-between items-center">
    <h1 class="font-bold text-4xl">Reviews</h1>
    <%= render 'shared/button/link', title: 'New review', path: new_review_path, twcss_color: 'bg-blue-100' %>
  </div>
  <div id="reviews" class="min-w-full">
    <%= render @reviews %>
  </div>
</div>
