<div class="flex flex-col mx-auto w-full gap-16">
  <div class='flex flex-row'>
    <%= render 'shared/button/link', title: 'Back to user', path: @user, twcss_color: 'bg-gray-100' %>
  </div>
  <h1 class="font-bold text-4xl">Editing User</h1>
  <%= render "form", user: @user %>
</div>
