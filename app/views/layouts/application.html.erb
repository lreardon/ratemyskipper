<!DOCTYPE html>
<html>
  <head>
    <title>Skipper Buoy</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind.css", "inter-font", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <%= favicon_link_tag asset_path('favicon.ico') %>
  </head>
  <body class='flex flex-col mx-auto bg-blue-100' style="max-width: 1600px;">
    <div class='min-h-screen bg-white'>
    <%= render 'layouts/navbar' %>
    <%= render 'shared/flashes' %>
    <main class="container mx-auto pt-5 pb-28 px-5 flex flex-col">
      <div class="flex flex-col">
        <%= yield %>
      </div>
    </main>
    <div>
  </body>
</html>
